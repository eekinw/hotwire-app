<main class="container">
  <%# sanitize is to prevent xss attacks, &larr is an arrow in html terms %>
  <%= link_to sanitize("&larr; Back to rooms"), rooms_path %>

  <div class="header">
    <h1>New room</h1>
  </div>

  <%= render "form", room: @room %>
</main>